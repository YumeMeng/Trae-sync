# Tickets: Trae Sync V1

这些纵向任务根据 `docs/IMPLEMENTATION_SPEC.md`、`docs/GATE_PLAN.md` 和 `docs/UI_PROTOTYPE_REVIEW.md` 构建 Trae Sync V1。

只处理依赖前沿：所有阻塞项完成后，ticket 才能交给执行 Agent。每次只执行一个 ticket；只有审查 Agent 可以修改完成状态。

统一验收规则：

- 执行 Agent 必须提交变更摘要、精确验证命令、测试输出、Gate 证据和未验证内容。
- Mock 只能证明 UI 或编排，不能替代 SQLCipher、文件、崩溃、Windows 或 TRAE UI 证据。
- Gate 未达到要求时，对应真实能力必须保持禁用或只读。
- 未经用户针对该 Gate 的明确批准，不读取或写入日常活动 TRAE 数据库。
- 任一测试不得自动删除用户历史、快照、备份或失败证据。

## T01 受保护的桌面应用骨架

**Status:** done

**What to build:** 用户可以启动一个空的 Trae Sync Windows 桌面应用，看到方案 D 的历史库骨架；开发者可以重复构建、测试和打包，同时测试环境无法把真实 TRAE 路径作为写入目标。

**Blocked by:** None — can start immediately.

- [x] Tauri 2、Rust、React、TypeScript 和 Vite 的最小生产骨架可在全新 checkout 构建。
- [x] Domain、application services、ports、infrastructure 和 Tauri command 的依赖方向符合实现规格。
- [x] Rust、TypeScript、前端测试、格式检查和 Windows 安装包构建均有单一文档化命令且通过。
- [x] `fixture_root` 只能位于测试根；默认 TRAE 路径、测试根外路径和路径别名写入均被自动测试拒绝。
- [x] 结构化日志包含 `operation_id`，且测试证明不会记录认证正文或密钥。
- [x] 空工作台可启动，真实数据能力保持禁用，并生成 Gate 0 PASS 证据。

## T02 Work CN 只读入口

**Status:** done

**What to build:** 用户可以选择 Work CN 数据位置并看到数据库兼容状态和当前账号；错误密钥、未知 schema、证据冲突或认证漂移只会进入明确的只读状态。

**Blocked by:** T01 受保护的桌面应用骨架。

- [x] 打包后端使用嵌入式 SQLCipher 打开 Work CN 副本和随机密钥目录库，不依赖外部 CLI、系统 SQLCipher 或 TRAE DLL。
- [x] 未 checkpoint WAL、错误 key、截断文件、未知 schema、事务提交/回滚和两层完整性检查均有真实 fixture 测试。
- [x] 账号解析器只读取白名单证据，区分 `userId` 与 `deviceId`，只持久化不可逆指纹。
- [x] 日志缺失、单来源、冲突、过期、Local Storage 冲突和指纹变化均返回结构化只读原因。
- [x] UI 始终显示平台、数据位置、当前账号或不可写原因，手工账号选择不能提升写权限。
- [x] Gate A 与 Gate B 均有 PASS 报告，诊断和持久化数据不含 Token、cookies 或认证正文。

## T03 授权扫描与首屏历史

**Status:** done

**What to build:** 用户明确授权并关闭 TRAE 后，可以把稳定的 DB/WAL/SHM 快照收入加密目录库，并在历史库中按账号、项目和完整对话浏览及搜索。

**Blocked by:** T02 Work CN 只读入口。

- [x] 自动扫描默认关闭；未授权、TRAE 运行中或文件集合变化时不会发布快照。
- [x] 快照按实际存在性捕获 DB/WAL/SHM，并记录文件身份、大小、哈希、schema、mapping 和账号证据引用。
- [x] 相同数据指纹不重复创建快照，成功快照发布后不可修改。
- [x] 随机密钥 SQLCipher 目录库保存最小规范化模型和加密搜索索引，磁盘无明文旁路索引。
- [x] 方案 D 的账号树、项目列表、对话标题和内容预览由真实目录库查询驱动；预览不会改变选择。
- [x] 应用服务级集成测试使用隔离 fixture，前端 Playwright 覆盖授权、空状态、扫描中、成功和失败状态。

## T04 历史语义稳定性

**Status:** done

**What to build:** 用户多次扫描和账号往返后仍能看到稳定来源、正确版本和可见历史；分叉、未知和软删除内容不会被覆盖或误计为可同步历史。

**Blocked by:** T03 授权扫描与首屏历史。

- [x] A/B 往返扫描保持 `first_observed_owner`，追加正确 observation，并稳定展示用户归类。
- [x] 内容图确定性区分 `Identical`、`FastForward`、`Forked` 和 `Unclassified`。
- [x] 行顺序、JSON 键顺序和派生缓存变化不会造成错误分叉；不确定版本不覆盖旧版本。
- [x] 软删除项目、会话和消息完整保留证据，但不进入普通统计和同步计划。
- [x] 同一逻辑会话不会重复，用户来源归类不修改快照或活动库。
- [x] Gate E、Gate I 和 Gate J 分别生成 PASS 证据。

## T05 同步范围与不可变计划

**Status:** done

**What to build:** 用户可以选择全部历史、账号、项目或完整对话，并在写入前看到准确的可同步动作、已在目标中的内容、排除原因和来源可见归属变化。

**Blocked by:** T04 历史语义稳定性。

- [x] 项目身份使用稳定证据，不把同路径、同标题或路径别名静默当作同一项目。
- [x] 完整项目生成 `FollowProject`；可靠目标项目中的选中会话生成 `AttachSessions`。
- [x] 目标项目不存在时，部分会话生成 `PartialProjectRequiresTarget`，只提供“选择整个项目”，不扩大范围。
- [x] 身份冲突、未知、归档、删除、schema 不兼容和版本不可用均形成明确排除项。
- [x] `SyncPlan` 固化账号、文件、schema、mapping、范围、动作、排除项和写前/写后断言，且不可变。
- [x] UI 分开显示“已选择”和“本次可同步”，确认目标账号并准确说明不是双账号复制。
- [x] Gate F 生成 PASS 证据。

## T06 双备份与完整项目安全执行

**Status:** ready-for-agent

**What to build:** 用户确认计划后，工具先创建并验证双备份，再以单事务执行完整项目跟随；成功必须经过新连接完整验证，失败必须保留证据或恢复到可证明状态。

**Blocked by:** T02 Work CN 只读入口；T05 同步范围与不可变计划。

- [ ] 原始 DB/WAL/SHM 证据和 SQLite Backup API 逻辑副本均按规范创建、哈希并独立验证。
- [ ] 固定恢复区 manifest 使用规定状态机，意图状态先于副作用持久化。
- [ ] `FollowProject` 使用参数绑定、预期来源约束、唯一约束和变更行数断言。
- [ ] 写前可取消且目标零修改；进入 `target_writing` 后取消和关闭不能破坏保护阶段。
- [ ] 提交后使用新连接完成 SQLCipher、SQLite 和业务关系验证，未通过时不显示成功或启动 TRAE。
- [ ] 写后失败保存目标现场、失败证据和写前备份；自动恢复只覆盖已证明状态，否则进入人工恢复。

## T07 会话重挂与崩溃恢复矩阵

**Status:** ready-for-agent

**What to build:** 用户可以把选中会话安全挂到目标账号已有的可靠项目；任一非终态发生进程终止后，重启都能得到唯一、可解释且不会重复写入的结果。

**Blocked by:** T06 双备份与完整项目安全执行。

- [ ] `AttachSessions` 只修改规格允许的项目关系，消息、正文 ID 和非目标记录保持不变。
- [ ] 未知引用表、目标 sandbox 不可用或关系断言不足时动作在写前被拒绝。
- [ ] 每个非终态均执行终止注入和重启协调，覆盖写前、已提交未验证、验证失败和恢复中断。
- [ ] 未提交结束为 `not_applied`，已验证提交协调为 `completed`，未知第三状态进入 `manual_recovery_required`。
- [ ] 不重复事务、不复用旧 WAL/SHM、不覆盖未知外部状态，所有备份和失败证据保留。
- [ ] Gate C 生成完整 PASS 证据并达到 `Qualified`。

## T08 数据位置、排他锁与证据漂移

**Status:** ready-for-agent

**What to build:** 多个数据位置、两个 Trae Sync 实例或外部账号切换同时存在时，用户仍不会把计划应用到错误账号、错误目录或已经变化的数据库。

**Blocked by:** T02 Work CN 只读入口；T07 会话重挂与崩溃恢复矩阵。

- [ ] 操作、计划、快照、备份和 manifest 全程绑定同一稳定 `data_location_id`。
- [ ] 目录库锁先于数据位置 OS 锁；第二实例只能浏览，不能启动副作用。
- [ ] 路径移动、同路径换目录、文件身份变化和错误目标进程均停止，不自动重定向。
- [ ] 预览后、事务前和提交前账号或文件证据漂移会使计划失效或回滚。
- [ ] 提交后漂移继续验证和协调，但不改变已确认目标、不自动启动 TRAE。
- [ ] Gate G 生成 PASS 证据。

## T09 生产桌面历史与安全任务流

**Status:** ready-for-agent

**What to build:** 普通用户可以在生产桌面应用中完成查找历史、浏览正文、选择范围、理解排除项、确认目标、执行、恢复和查看操作证据的完整流程。

**Blocked by:** T05 同步范围与不可变计划；T07 会话重挂与崩溃恢复矩阵；T08 数据位置、排他锁与证据漂移。

- [ ] 方案 D 信息架构通过结构化 command 和 operation event 接入真实应用服务，不复用原型单组件结构。
- [ ] Playwright 覆盖 Gate D 所列状态，包括计划漂移、不可取消阶段、恢复结果、空间不足和另一实例操作中。
- [ ] 目标账号在标题栏和确认区复核；来源可见归属变化、排除项和可同步数量无歧义。
- [ ] 设置首页只有“自动查找新历史”和“成功后重新打开 TRAE”；自动查找默认关闭。
- [ ] 长中文、路径、错误和目标窗口尺寸无重叠、截断或水平溢出。
- [ ] 打包应用完成 Windows 手工工作流，Gate D 生成 PASS 证据。

## T10 隔离 TRAE 会话重挂验收

**Status:** ready-for-agent

**What to build:** 在用户明确批准的隔离数据位置中，证明重挂后的会话可以在真实 TRAE 列表和搜索中出现、打开正文并继续发送消息，然后反向重挂仍保持完整。

**Blocked by:** T05 同步范围与不可变计划；T07 会话重挂与崩溃恢复矩阵；T08 数据位置、排他锁与证据漂移。

- [ ] 使用隔离位置和已验证完整备份，不操作日常活动库。
- [ ] 单条、多条及各类关联 fixture 完成列表、全局/项目搜索、正文、工具调用和附件检查。
- [ ] 旧会话继续发送一轮后，新旧内容、关系和两层完整性检查均通过。
- [ ] 目标原会话和不同 `session_id` 全部保留，sandbox 不明确的类型保持禁用。
- [ ] 反向重挂重复关键验证并通过。
- [ ] Gate M 生成用户可核对的截图、数量、哈希和 PASS 证据。

## T11 大库长任务与响应性

**Status:** ready-for-agent

**What to build:** 用户处理约 257 MB 历史副本和较慢磁盘时，可以看到真实阶段和进度，界面持续响应，内存有界，取消不会破坏数据保护。

**Blocked by:** T09 生产桌面历史与安全任务流。

- [ ] 复制和哈希显示真实字节进度；完整性检查等未知总量阶段不显示虚假百分比或 ETA。
- [ ] 事件节流，前端保持可交互、可最小化和恢复。
- [ ] 扫描、Backup API、验证、失败现场和恢复 staging 均记录耗时与峰值内存。
- [ ] 不把完整数据库无界读入内存；进入写入后取消不可用，写前取消目标零修改。
- [ ] Gate K 在目标副本和慢盘场景生成 PASS 证据。

## T12 存储根生命周期与手工删除

**Status:** ready-for-agent

**What to build:** 用户可以安全迁移历史库存储根并手工管理空间；掉线、空间不足、崩溃或删除中断不会产生双权威库、空库替代或删除范围外对象。

**Blocked by:** T07 会话重挂与崩溃恢复矩阵；T11 大库长任务与响应性。

- [ ] 峰值空间公式经过实测校准，`target_writing` 前完成实际预留，空间不足时零目标写入。
- [ ] 新根完整复制、逐文件校验和目录库验证后才切换指针；旧根只读保留。
- [ ] 根缺失、掉线、ID 不匹配和迁移各阶段崩溃后只有一个可写权威根，不创建空目录库。
- [ ] 5 GB 警戒线只暂停非必要自动扫描，不自动删除数据。
- [ ] 删除前展示精确影响，保护对象需显式解除保护并二次确认；取消时零删除。
- [ ] 删除计划漂移、逐对象中断和继续操作不扩大范围，并保留一致审计墓碑。
- [ ] Gate L 生成迁移、空间和删除场景的 PASS 证据。

## T13 目录库恢复与旁路升级

**Status:** ready-for-agent

**What to build:** 用户在 DPAPI 丢失或应用升级后仍能安全打开正确目录库；错误密码、错误库、损坏包或迁移失败不会覆盖现有历史或恢复状态。

**Blocked by:** T03 授权扫描与首屏历史；T07 会话重挂与崩溃恢复矩阵。

- [ ] 密码恢复包使用经基准固定的版本化 KDF 和认证加密，不保存正文、明文密钥或密码。
- [ ] 错误密码、损坏包、库 ID/密钥代次不匹配时零文件变化；正确恢复先只读验证再生成 DPAPI 包装。
- [ ] 目录库升级复制到 staging、逐级迁移、两层完整性和语义数量验证后原子切换。
- [ ] 升级各阶段中断继续使用旧库兼容只读，旧代不修改、不自动删除。
- [ ] 固定 recovery manifest 不依赖升级后的新目录库。
- [ ] Gate H 与 Gate N 分别生成 PASS 证据。

## T14 Windows V1 候选版本

**Status:** ready-for-agent

**What to build:** 用户可以在干净 Windows x64 环境安装 Trae Sync，并按正式流程完成扫描、浏览、计划、备份、应用、验证、恢复、迁移和恢复包演练，未通过 Gate 的能力不会被发布。

**Blocked by:** T09 生产桌面历史与安全任务流；T10 隔离 TRAE 会话重挂验收；T11 大库长任务与响应性；T12 存储根生命周期与手工删除；T13 目录库恢复与旁路升级。

- [ ] 干净 Windows 安装包检测 WebView2，可打开目录库并完成只读扫描。
- [ ] V1 Release Gate 的全部端到端步骤和往返验证完成。
- [ ] 所有启用能力对应 Gate 均为 `Qualified`，mock、Research 和 source evidence 未被冒充为真实验收。
- [ ] 没有自动删除、错误账号/位置写入、重复计划或半成功状态。
- [ ] 发布说明、UI 能力和已知限制一致，安装包不依赖开发机 SQLCipher、Docker 或 TRAE DLL。
- [ ] 最终 Gate 报告列出精确命令、环境、哈希、截图、未验证内容和恢复状态。
