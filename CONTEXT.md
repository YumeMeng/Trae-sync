# Trae Sync

个人使用的 TRAE Work CN 多账号日常管理工作台。四个核心需求：**账号保存**（凭据持久化）、**对话主库**（所有账号共用一套历史记录）、**直连签到**（HTTP 完成每日签到）、**账号切换**（集中管理、快速切换、显示信息与积分）。

项目方向最高依据：`docs/DECISIONS-20260822-GRILL.md`（2026-08-22 决策记录）。旧"受保护历史库 + Gate 体系"定位已废止，相关文档已于 2026-08-22 彻底清理（git 历史可查）。

## 领域语言

### 账号与凭据

**账号档案**：一个 TRAE Work CN 账号在本 App 中的完整登记：用户 ID、昵称、凭据包、设备身份、data_dir 绑定与签到状态。账号一律通过 App 内 OAuth 登录建立，不从外部工具导入。

**凭据包**：单个账号的 DPAPI 加密凭据容器，包含 access token、refresh token、设备 ID、设备密钥对、MachineID 与必要客户端属性。凭据正文不进入前端 DTO、日志或导出；续期轮换按 ADR-0014 的原子写回执行。

**虚拟设备身份**：与账号绑定的服务端设备身份：16 位设备 ID + 随机 MachineID + EC P-256 密钥对成套生成，可通过 OAuth 登录或 AuthCode 重铸（见 ADR-0019 v5）建立，随 `ExchangeToken` 注册。该账号的签到与续期永远使用此身份；设备失效（9074/9095）时自动重铸新设备替换。

**登录凭据存档**：账号在 App 存储根下的登录凭据保存形态（`trae-instances\{profile_id}` 目录三件套），是切号的凭据来源与保活的写回对象。切号对供体目录的全部消费仅此存档（E1 实证，ADR-0024）；存档不承担运行实例职责。

**环境**：一个独立 TRAE 数据目录与当前登录账号的组合。主库环境绑定官方目录（默认、不可删除）；副环境由用户自建，承载隔离的任务场景。任意账号可登录到任意环境（多对多）；环境内记录单一归属当前登录账号；环境间可并行运行，环境内单实例（ADR-0024）。

**实例**：一个环境中运行的 TRAE 进程（每环境至多一个），由环境统一启动与关闭。账号专属实例形态已退役（ADR-0024）。

### 主库

**主库**：主库环境（官方数据目录）内全部对话记录的集合，不是某个账号的内容。库内记录单一归属当前登录账号：切号时全部记录自动随行，任何账号登录后看到的都是同一套完整历史。见 ADR-0021。

**主库环境**：绑定官方数据目录的默认环境（`env_id=master`），全部对话记录的唯一承载，不可删除；副环境为 Phase 6 落地内容（ADR-0024）。

**归属随行**：切号事务把全部记录的归属改写为新登录账号（含空壳镜像清理与活跃会话换腿），单一归属保持不变。

**收编**：不切号的情况下，把库内不属于当前账号的记录（含未注册账号的历史遗留）一键归入当前账号；由环境页「主库体检」区块发起。

**主库体检**：环境页的只读健康视图（账号分布、未注册账号标注、空壳镜像等异常）+「归入当前账号」入口。

**会话归档**：把会话从 TRAE 会话列表移出而不删除的收纳操作（口语：出库/入库）。出库后 TRAE 不再显示该会话，工具的归档抽屉按原层级（模式 → 分组 → 会话）收纳并支持随时恢复；归档不改变记录归属，切号随行不受影响。

**环境插件清单**：每个环境持有的插件绑定基线。切号时与账号云端对账：先吸收当前账号的增删差异，再把清单应用到目标账号；工具内的插件管理操作即时修改当前账号云端。清单是同步基线与恢复依据，不是审批闸门。

### 签到

**一键签到**：对选中账号建立签到认证上下文并直接执行签到请求（status → 唯一一次 claim → status 复核），不启动 TRAE 进程。不是后台定时任务。

**设备重铸**：为账号铸造全新签到设备并写回凭据（当前 token → GetPCAuthCode → 新设备四件套 → ExchangeToken → 账号匹配校验 → 原子写回）。两条触发路径：签到被 9074/9095 拒绝时自动重铸（冷却 3 分钟后重试一次）；用户在账号详情手动重铸（自动路径失败后的兜底）。失败零回写，旧凭据保持可用。

**打开时自动签到**：App 启动时检测各账号今日签到状态并自动执行未完成签到的设置项（默认开启，可关闭）。Token 续期检查同时执行。

**签到结果**：`claimed`（本次真实领取成功）、`already_checked_in`（今日已完成）、`not_eligible`（9074/9095 等业务拒绝）、`auth_mismatch`（认证失效）、`runtime_error`、`verification_failed`。HTTP 200 不构成成功；判定真实发放必须看 `checked_in` 翻转（claim 对当日已签账号返回 code=0 但不发放，幂等空操作）。

**签到恢复**：请求中断或响应不完整时，先用只读 `status` 复核最终状态；不得自动重复 `claim`（重铸重试是全新设备的全新 claim，不违反此铁律）。

**签到证据**：说明目标账号、设备身份、执行时序与最终结果的本地记录；保存脱敏账号标签、业务状态、积分变化、业务码与时间，不含 Token、Cookie 或完整认证对象。

### 数据与存储

**数据位置**：一个明确的 TRAE Work CN 数据目录及其数据库文件集合，具有稳定身份。

**存储根**：Trae Sync 管理自有数据（主库、目录库、备份）的物理根目录。

**TRAE 原始数据库 raw key**：TRAE Work CN 数据库的项目技术参数（详见 TECHNICAL_BASELINE），用于读取账号库与主库读写。

**手动备份入口**：App 内由用户主动操作的备份功能：自选位置、内容（账号凭据/历史库/主库）与时机。这是红线解除后唯一的数据安全保障形态（ADR-0018），破坏性批量操作另需单次确认。

## 关键协议事实（实测固化）

签到服务三层规则（2026-08-20/21 实验，证据 `.scratch/checkin-http/`）：

1. `checked_in`（status 展示）：账号维度，CST 午夜重置。
2. **9074 陌生设备门禁**：设备 ID 无成功签到记录时 claim 被拒；**新设备首签仅 SOLO 客户端形态可行**（en1oxy7wnw8j9n / SOLO_PC / 完整硬件字段——2026-08-26 实测，Work 形态新设备被稳定拒绝）；9074 另含**频率维度**：短时连续铸造+首签会被拒，冷却 3-10 分钟后重试可过。
3. **9095 设备日配额**：同一 x-device-id 每日限一次；每账号独立设备自用自签，配额天然不冲突。

**设备重铸**（2026-08-25 手册 + 2026-08-26 全账号实证）：任何账号的 access token + `x-cloudide-token` 头可请求 `GetPCAuthCode` 为全新设备签发 AuthCode，经 `ExchangeToken(AuthCode+PKCE)` 换取新设备绑定的新凭证，无需浏览器。设备失效时的应对从"降级借池"改为"重铸新设备"（ADR-0019 v5）。

无账号级冷却（距上次成功 4.3 小时即再发放）、无 IP 配额（同 IP 同日多次成功）。本机所有 TRAE 客户端共用同一硬件指纹设备 ID（`EE5A02EE69AAA646` 收敛实证），这是多账号直连签到需要虚拟设备的原因。

OAuth/ExchangeToken/GetPCAuthCode/请求头构造/byteCrypto 解密等协议细节见 `docs/TECHNICAL_BASELINE.md` 与 `.scratch/checkin-http/` 下的复现手册。

## 架构资产现状

- **crate 分层**：domain / application / infrastructure / ports + 组合根（保留，继续使用）
- **可用实现**：签到 HTTP 协议层、OAuth 登录流程、DPAPI 凭据包、EC 设备密钥对、SQLCipher 读写资产、桌面 UI 四页骨架（Overview/History/Accounts/Settings）
- **业务码透传**（2026-08-22 已修复，P0-2）：`detail_code` 以 `business_{code}` 形态直达前端（9074/9095/20324 分别可辨），claim 业务码拒绝按 `not_eligible` 分类（此前误报 `verification_failed`）；协议层注释已对齐 9074/9095 双闸门事实
- **待清理**：Gate 证据体系代码（evidence 收集、资格判定）随模块改造逐步移除，不做一次性大删除

## 文档地图

| 文档 | 用途 |
|------|------|
| `docs/DECISIONS-20260822-GRILL.md` | 项目方向历史锚点（九问九答全量决策；部分主库/实例决策已被 ADR-0021/0024 取代，见其头部标注） |
| `docs/adr/README.md` | ADR 索引与维护规则（任务驱动新增） |
| `docs/adr/0018-0024` | 现行架构决策：红线解除 / 签到虚拟设备与账号来源 / 主库与实例模型 / 主库记录单一归属 / 会话归档通道 / 环境插件清单 / 账号-环境解耦 |
| `docs/DESIGN_TOKENS.md` | UI 视觉与交互契约（毛玻璃设计系统，UI 三批改造的开发规范） |
| `docs/TECHNICAL_BASELINE.md` | 协议与技术事实基线（含 SQLCipher 导出契约） |
| `tickets.new.md` | Phase 0-7 实施计划（现行唯一计划文件；旧版 `tickets.md` 已于 2026-09-02 删除） |
| `.scratch/checkin-http/` | 签到实验证据与复现手册 |
