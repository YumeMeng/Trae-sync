{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Trae Sync 默认窗口能力：允许调用 get_workspace_state 命令",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
